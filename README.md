# Prerequisites
- nginx
- node 4.5.0

# Installation:
- `npm install`
- `touch .env`
- Copy and edit contents of `.env.example` to `.env`

# Usage:
- `sudo forever start index.js`

To stop:
`sudo forever stopall`
